import OBSDashboard from "@/components/OBSDashboard";

export const metadata = {
  title: "OBS Dashboard | Streaming Terminal",
  description: "Control OBS Studio via WebSocket: scenes, stream, and recording.",
};

export default function OBSDashboardPage() {
  return <OBSDashboard />;
}
